extends Resource
"""
this is a config file, for globably loading (i think) all enemy stats across the game. 
it was in the tutourial, so i might null it if doesnt do anything

"""

class_name enemy_resource

@export var sprite : Texture2D
@export var width: int
@export var crystals: int
# @export var animation_name: String
# redundant for now, as there's no animations, but might use later.
